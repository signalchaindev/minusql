{
  "name": "minusql-suite",
  "version": "1.0.0",
  "repository": {
    "type": "git",
    "url": "git@scdgh:signalchaindev/minusql.git"
  },
  "scripts": {
    "dev": "run-p api sapper minusql",
    "minusql": "pushd minusql && npm run dev",
    "api": "pushd api && npm run dev",
    "sapper": "pushd sapper && npm run dev",
    "//svelte": "pushd web && npm run dev",
    "//static": "pushd examples\\static && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "npm-run-all": "4.1.5"
  }
}
